import 'package:flutter/material.dart';

class EHP extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('EHP'),
        centerTitle: true,
      ),
    );
  }
}