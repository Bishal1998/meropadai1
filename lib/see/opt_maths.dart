import 'package:flutter/material.dart';

class Omaths extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Optional Maths'),
        centerTitle: true,
      ),
    );
  }
}